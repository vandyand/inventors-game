import React from "react";
import SquareGrid from "../grids/SquareGrid";

type Props = any;

const GridWindow = (props: Props) => {
  return (
    <div>
      <SquareGrid />
    </div>
  );
};

export default GridWindow;
